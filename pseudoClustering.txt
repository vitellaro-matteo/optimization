class node
	int id
	node[] neigbhours
	boolean head = false
	double weight = randomNumber(k, 0)
	double max = 0
	double min = k

node[] nodes = [all]
for int i = 0;i >= roundup(nodes.size*0,3); i++
	boolean changing = true
	while(changing)
		int random = randomNumber
		if nodes[random].head = false 
			nodes[random].head = true
			changing = false
		

